services:
  backend:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./draft_scripts:/app/draft_scripts
      - ./story_notes:/app/story_notes
    environment:
      - DB_PATH=/app/data/scify.db
