# TODO

- Encoding rules currently apply globally (no `document_id` on `encoding_rules`), so every rule shows regardless of which document is loaded. Not all rules apply to all documents - add a way to enable/disable individual rules per document.
